# Local app commands no longer read `app/.env`.
# Use the repository root `.env` instead:
#
#   cp ../.env.example ../.env
#
# `vite`, `npm run server`, `npm run db:migrate`, and `npm run dev:local-db`
# now all load environment variables from the root `.env` automatically.
