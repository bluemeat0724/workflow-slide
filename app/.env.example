# Local app commands no longer read `app/.env`.
# Use the repository root `.env` instead:
#
#   cp ../.env.example ../.env
#
# `npm run dev`, `npm run frontend`, `npm run server`, and
# `npm run db:migrate` load environment variables from the root `.env`.
#
# STORAGE_DRIVER is required and must be "sqlite" or "postgres".
